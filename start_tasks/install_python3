/bin/bash -c 
"sudo apt-get update && 
 sudo apt-get install python3.6 &&
 sudo apt-get install -y python3-pip &&
 python3 -m pip install -r requirements.txt" &&
 cd $AZ_BATCH_NODE_SHARED_DIR &&
 tar -xvf $AZ_BATCH_NODE_STARTUP_DIR/wd/myapp.tar.gz"
